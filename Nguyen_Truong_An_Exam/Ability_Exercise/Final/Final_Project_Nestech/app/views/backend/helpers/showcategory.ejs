<% 

    showCategory = (collection, id) => {
        if (collection.length == 0) {
            return `Null`;
        } else {
            let result = collection.find(obj => {
                return obj._id == id
              })
            return result.name
        }
    }
    
%>
